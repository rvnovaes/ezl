FROM python:3

RUN apt-get update && apt-get install -y \
    postgresql-contrib \
    freetds-dev \
 && rm -rf /var/lib/apt/lists/*


RUN mkdir -p /app /var/log/ezl/etl/
WORKDIR /app
COPY ["requirements/development.txt", "requirements/production.txt", "./"]

RUN pip install -r development.txt